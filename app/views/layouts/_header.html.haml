- if controller_name != 'pages' || params[:id] != 'safety'
  %div#system-notification.row
    %p
      <b>July 6 Update</b>: We have isolated many complaints to a &#8220;Patricia Muller&#8221; (patriciamuller88@gmail.com). Visit our #{link_to 'Safety & Security page', safety_page_path} to learn more.
      %a#dismiss-system-notification{:href => '#', :title => 'Dismiss this message'} [dismiss]
%header.row
  %h1#marketplace-logo.threecol= root? ? SITE_NAME : link_to(SITE_NAME, :root)
  - if content_for? :top
    %div#top.ninecol.last
      = yield :top
  - else
    %div#marketplace-search.ninecol.last
      = render 'layouts/simple_search', :search => @search
  - unless flash.empty?
    #flashes.twelvecol.last
      - flash.each do |type, text|
        %p.flash{:class => type} <strong>#{type.capitalize}</strong>: #{text}
