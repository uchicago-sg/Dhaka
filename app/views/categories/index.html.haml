- title 'Atom Feeds'
- content_for :sidebar do render 'pages/sidebar' end
%h1 Atom Feeds
%ul.categories
  %li= link_to 'All Categories'.html_safe, categories_path(:format => :atom)
  = render @categories
- if can? :create, Category
  %ul.actions
    %li.new= link_to 'New Category', new_category_path