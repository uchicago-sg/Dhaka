%li.category
  %p
    #{link_to category.description, category_path(category, :format => :atom)} - #{category.details}
    - if can? :manage, category
      (#{link_to 'Show', category} | #{link_to 'Edit', edit_category_path(category)} | #{link_to 'Destroy', category, :method => :delete})